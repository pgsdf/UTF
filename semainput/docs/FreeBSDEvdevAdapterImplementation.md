# v28 notes

## New behavior

- emits newline-delimited JSON output
- semantic events are now structured instead of free-form debug text
- gesture events are now structured instead of free-form debug text
- retains multitouch scroll hysteresis
- retains drag suppression during active multitouch scroll
- retains smoothed two-finger scroll output
- retains touch marker key filtering
